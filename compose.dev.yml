services:
    friendly-web-dev:
        image: "friendly-web-dev:latest"
        ports:
            - "${PORT}:3000"
        environment:
            - NODE_ENV=production
        restart: unless-stopped
