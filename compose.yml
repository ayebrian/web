version: "3.8"

services:
    friendly-web:
        image: friendly-web:latest
        ports:
            - "${PORT}:3000"
        environment:
            - NODE_ENV=production
        restart: unless-stopped
